{
  "name": "react-palette-pad",
  "version": "0.0.3",
  "description": "color picker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "./node_modules/.bin/rimraf lib",
    "build": "./node_modules/.bin/babel src --copy-files --source-maps --extensions .js --out-dir lib"
  },
  "keywords": [
    "color",
    "picker",
    "colorpicker",
    "react",
    "palette",
    "pad",
    "colour"
  ],
  "author": "gim",
  "license": "MIT",
  "dependencies": {
    "react-color-class": "0.0.6"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1"
  }
}
